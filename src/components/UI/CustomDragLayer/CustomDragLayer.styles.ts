import styled from 'styled-components';

const CustomDragLayerContainer = styled.div`
  height: 100%;
  left: 0;
  top: 0;
  pointer-events: none;
  position: fixed;
  width: 100%;
  z-index: 100;
`;

export { CustomDragLayerContainer };
